import styles from "@/assets/styles/components/Tracker.module.css";

export const Tracker = (): React.JSX.Element => {
    return (
        <section>
            <div className={styles.containerBackground}>
                <div className={styles.container}>
                    <h2>Tracker</h2>
                </div>
            </div>
        </section>
    );
};
