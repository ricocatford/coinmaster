export default function TrackerPage(): React.JSX.Element {
    return (
        <section className="container fullWidth borderBottom">
            <h1 className="heading">Tracker</h1>
        </section>
    );
}
